# AWS Configuration
# ================
# Option 1: Use AWS CLI credentials (recommended)
# No need to set these if using AWS CLI profiles
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here
# AWS_SESSION_TOKEN=your_session_token_here  # Only for temporary credentials
# AWS_DEFAULT_REGION=us-east-1

# Option 2: Use AWS IAM Role (if running on EC2 or with IAM roles)
# No credentials needed - AWS will use the instance profile

# Terraform Configuration
# ======================
TF_VAR_environment=production
TF_VAR_project=BOA_Beta_v3
TF_VAR_owner=coldchainsecure
TF_VAR_cost_center=IT

# Build Configuration
# ==================
APPLY=false  # Set to true to apply changes

# Security Notes:
# ==============
# 1. Never commit real credentials to version control
# 2. Use AWS CLI profiles for local development
# 3. Use IAM roles for production deployments
# 4. Consider using AWS Secrets Manager for sensitive data
# 5. Rotate credentials regularly 